{
	"name": "hono-backend",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "node -r dotenv/config ./node_modules/wrangler/bin/wrangler.js deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"test:env": "echo $CLOUDFLARE_API_TOKEN"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.22.0"
	},
	"dependencies": {
		"dotenv": "^16.5.0",
		"hono": "^4.8.3"
	}
}
